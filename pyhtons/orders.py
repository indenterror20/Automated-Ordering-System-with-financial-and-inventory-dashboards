# fetch api 

#do the process
class Orders:
    def__init__(Orders, OrderId, OrderDate, CustomerName, OrderType, TotalItems, OrderTotal, PaymentStatus, OrderStatus)
    Orders.OrderId = OrderId
    Orders.OrderDate = OrderDate
    Orders.CustomerName = CustomerName
    Orders.OrderType = OrderType
    Orders.TotalItems = TotalItems
    Orders.OrderTotal = OrderTotal
    Orders.PaymentStatus = PaymentStatus
    Orders.OrderStatus = OrderStatus

    

#send it into javascript (visual datas)